'use client';

// ── Week 3 TODO: Convert questions screen ─────────────────────────────
// AI-generated clarifying questions from CLARIFY prompt.
// No maxSelections cap (data-max="999"). Other text input per question.

export default function QuestionsPage() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 24,
      }}
    >
      <div className="card" style={{ maxWidth: 480, width: '100%', textAlign: 'center' }}>
        <p style={{ color: 'var(--txt2)', fontSize: 14 }}>
          Questions screen · Week 3 conversion
        </p>
      </div>
    </main>
  );
}
